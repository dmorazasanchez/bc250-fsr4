# Vulkan CTS groups most relevant to the sdot_4x8_a_b (signed INT8 4-way
# dot-product lowering) change in nir_opt_algebraic.py.
#
# These are `--deqp-case` wildcard patterns. Each line is one pattern and is
# passed to deqp-vk, which runs every matching leaf (so group names need not
# enumerate individual cases, and never go stale).
#
# The BC-250 here is used as a gaming machine running a Wayland compositor,
# so the dEQP-VK.wsi.wayland presentation group is exercised too (surface /
# swapchain / present paths used by real gaming workloads). Headless boxes can
# override at runtime with:  ./cts-conformance.sh --full
dEQP-VK.spirv_assembly.*
dEQP-VK.compute.*
dEQP-VK.shaders.*
dEQP-VK.pipeline.*
dEQP-VK.robustness.*
dEQP-VK.wsi.wayland.*
